# Gloomberb 简体中文汉化日志

## 2026-07-21 · 第二轮汉化补充

分支：`zh-cn-localization`

### 本次完成

- 补充券商连接向导：设置指南、字段步骤、默认值、选择提示、同步状态和重试提示。
- 补充 Gloom Cloud 账户管理：账户资料、邮件提醒、公开统计、Pro 套餐、密码修改、删除账户及状态提示。
- 补充 Chat：只读提示、登录提示、加载状态、私信/群聊、新消息、回复、编辑和输入框提示。
- 补充 Broker Manager：资料状态、账户区域、操作按钮、连接/同步/删除提示、表格列名。
- 补充 AI 和 AI Screener：提问、生成、选股器编辑、刷新、运行状态和空状态。
- 补充 Cloud 验证邮箱提示。
- 将新增界面文字统一接入 `t()` / `tf()`，中文缺失时仍会自动回退到英文。

### 审计结果

- 中文词典从约 576 条增加到 858 条。
- 显式 `t()` / `tf()` 调用静态审计：缺失映射 0 条。
- 金融缩写、股票代码、品牌名、URL、命令名和用户输入内容按设计保留英文。

### 验证

- `bun run build`：通过。
- 相关模块测试：60 个通过，0 个失败。
- 全量测试：1450 个通过，1 个失败；失败是原有 `src/renderers/electrobun/bun/desktop/http-fetch.test.ts` 的手动重定向测试，期望 302、实际收到 503，与本轮汉化无关。
- 继续补充其他业务插件前，需重新运行对应测试和完整构建。

### 待处理

Alerts、Analytics、Options、Research、SEC、Substack、13F、Earnings、经济日历、收益率曲线等业务插件仍有部分英文加载提示、空状态、字段名和命令描述，留待下一轮处理。

## 2026-07-21 · 第三轮汉化：主要看板

### 本次完成

- 顶部应用栏：更新下载、安装、检查更新、更新失败和可用版本提示。
- 底部状态栏：命令栏提示、窗口吸附提示、全部网格对齐和忽略按钮。
- 组合列表：现金与保证金抽屉、现金余额空状态、快捷添加股票、表格空状态和列标题。
- 股票研究：研究页签、概览页标题、行情字段、基本面字段、持仓字段和图表加载/渲染提示。
- 保留用户自定义组合名、股票名称、股票代码、金融缩写、品牌名和外部行情内容为原文。

### 审计结果

- 中文词典现有 952 条。
- 显式 `t()` / `tf()` 调用静态审计：494 个调用，缺失映射 0 条。

### 验证

- 主看板相关测试：65 个通过，0 个失败。
- `bun run build`：通过，macOS ARM64 打包二进制签名和冒烟测试通过。
- 本机 Electrobun 开发窗口已随文件变更自动重新构建并启动，继续使用 `GLOOMBERB_LANG=zh-CN`。
