Stack: Bun + OpenTUI

Important:
- Use tmux to test your changes (see the `tui-testing` skill). Always kill the tmux session when done.
- Aim for mouse/cursor interactivity for everything interactive.
- Never "fix" chart issues by disabling or forcing off the kitty chart renderer; preserve kitty support and fix the root cause.
